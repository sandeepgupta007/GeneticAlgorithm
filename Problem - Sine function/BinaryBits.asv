function [ b ] = BinaryBits( x,y )
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here
a = (y-x)*10000;
b = log2(a);
if floor(b) == b
    b = floor(b)
    return b
else
    b = floor(b)+1
    return b
end
end


